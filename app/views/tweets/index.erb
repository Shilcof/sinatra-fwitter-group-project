<p> Welcome, <%= current_user.username %>
<h2> All Tweets </h2>

<% @tweets.each do |tweet| %>

    <p><a href="/tweets/<%= tweet.id %>"><%= tweet.user.slug %></a><%= tweet.content %></p>
    
<% end %>